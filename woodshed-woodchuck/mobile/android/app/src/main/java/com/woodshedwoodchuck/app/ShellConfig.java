package com.woodshedwoodchuck.app;

/** The only native origin setting. Changing it requires a reviewed binary build. */
final class ShellConfig {
    static final String ORIGIN = "https://woodshed-woodchuck.onrender.com";
    private ShellConfig() {}
}
